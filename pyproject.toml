[project]
name = "pokeloop"
version = "0.1.0"
description = "AI plays Pokémon and DPOs itself, on a forkable VM"
requires-python = ">=3.11"
dependencies = [
  "anthropic>=0.40",
  "fastapi>=0.110",
  "httpx>=0.27",
  "pydantic>=2.6",
  "pyboy>=2.0",
  "uvicorn[standard]>=0.27",
  "websockets>=12",
]
